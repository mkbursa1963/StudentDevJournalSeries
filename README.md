Projeyi yerel ortamda çalıştırmak için aşağıdaki adımları sırayla uygulayın.

### 1. Ortam Değişkenleri (Environment Variables)

Bu projede bazı yapılandırmalar (port, JWT secret vb.) ortam değişkenleri üzerinden yönetilir.  
Öncelikle `.env.example` dosyasını kullanarak bir `.env` dosyası oluşturun:
